#include <iostream>
using namespace std;
int main(int argc, char const *argv[]) {
  queue<char> s;
  int n;
  while (scanf("%d",&n )!=EOF) {
    char *a=new char[n];
    for (int i = 0; i < n; i++) {
      std::cin >> a[i];
      s.push(a[i]);
    }
    string t;
    while (s.size()) {
      t+=s.
    }
  }
  return 0;
}
